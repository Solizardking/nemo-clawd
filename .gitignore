node_modules/
nemo-clawd-mcp/node_modules/
# ── Dependencies ──────────────────────────────────────────────────
node_modules/
dist/
!nemoclawd/dist/
.pytest_cache/
__pycache__/
*.pyc
# ── Dependencies ──────────────────────────────────────────────────
node_modules/
dist/
!nemoclawd/dist/
.pytest_cache/
__pycache__/
*.pyc
.env.local
# ── OS / Editor ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/
.env.local
# ── Documentation build ──────────────────────────────────────────
docs/_build/
.env 
# ── Secrets & Credentials (CRITICAL) ─────────────────────────────
# Environment files (may contain API keys, tokens, private keys)
.env
.env.*
!.env.example
!.env.template

# NemoClaw local configuration (contains API keys, wallet refs)
.nemoclawd/
*.nemoclawd.json

# Privy wallet configuration (contains app secrets)
privy.json
privy-wallets.json

# Solana keypairs and wallet files
*.keypair
*.keypair.json
*-keypair.json
id.json
solana-wallet.json
test-ledger/

# Generic secret patterns
auth.json
credentials.json
secrets.json
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Helius / RPC config with embedded keys
helius.json
rpc-config.json

# npm auth token (NEVER commit)
.npmrc
!core-ai/.npmrc

# ── Build artifacts ───────────────────────────────────────────────
*.tgz
*.tar.gz
coverage/
.nyc_output/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Docker ────────────────────────────────────────────────────────
.docker/
docker-compose.override.yml

# Local Netlify folder
.netlify

# ── OS / Editor ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# ── Documentation build ──────────────────────────────────────────
docs/_build/

# ── Secrets & Credentials (CRITICAL) ─────────────────────────────
# Environment files (may contain API keys, tokens, private keys)
.env
.env.*
!.env.example
!.env.template

# NemoClaw local configuration (contains API keys, wallet refs)
.nemoclawd/
*.nemoclawd.json

# Privy wallet configuration (contains app secrets)
privy.json
privy-wallets.json

# Solana keypairs and wallet files
*.keypair
*.keypair.json
*-keypair.json
id.json
solana-wallet.json
test-ledger/

# Generic secret patterns
auth.json
credentials.json
secrets.json
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Helius / RPC config with embedded keys
helius.json
rpc-config.json

# npm auth token (NEVER commit)
.npmrc
!core-ai/.npmrc

# ── Build artifacts ───────────────────────────────────────────────
*.tgz
*.tar.gz
coverage/
.nyc_output/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Docker ────────────────────────────────────────────────────────
.docker/
docker-compose.override.yml

# Local Netlify folder
.netlify

.env
.env.*
!.env.example
credentials.json
*.keypair.json
*.secret

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store

__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.venv/

docs/_build/

services/**/node_modules/
services/**/dist/
services/**/.agent/
services/**/.surfpool/
services/**/.logs/
services/**/logs/
services/**/output/
